[package]
name = "dedupe_backend"
version = "1.3.9"
edition = "2021"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dedupe_core = { path = "../core" }
dedupe_job_runner = { path = "../job_runner" }

[dev-dependencies]
tempfile = "3"
