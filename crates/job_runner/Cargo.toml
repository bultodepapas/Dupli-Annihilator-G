[package]
name = "dedupe_job_runner"
version = "1.2.0"
edition = "2021"

[dependencies]
anyhow = "1"
dedupe_core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
